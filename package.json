{
  "name": "reverx99-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node build.mjs && node tools/test.mjs",
    "dev": "node build.mjs && node --watch-path=./content --watch-path=./themes build.mjs",
    "serve": "node build.mjs && python3 -m http.server 8099 --directory docs --bind 127.0.0.1"
  }
}
